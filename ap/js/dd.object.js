function DragObject(element, handler) {	element.dragObject = this;	dragMaster.makeDraggable(element);	var rememberPosition,mouseOffset;		this._ = element;	this.onDragStart = function(offset) {		var s = element.style;		rememberPosition = {top: s.top, left: s.left, position: s.position};		s.position = 'absolute';		s.cursor = 'move';		mouseOffset = offset;	}			this.hide = function() {		element.style.display = 'none' ;	}		this.show = function() {		element.style.display = '' ;	}		this.onDragMove = function(x, y) {		element.style.top =  y - mouseOffset.y +'px';		element.style.left = x - mouseOffset.x +'px';	}		this.onDragSuccess = handler; //function(dragObject,dropTarget) { console.log('success'); }		this.onDragFail = function() {		console.log('test1');		var s = element.style;		s.cursor = '';		s.top = rememberPosition.top;		s.left = rememberPosition.left;		s.position = rememberPosition.position;	}		this.toString = function() {		return element.id;	}}